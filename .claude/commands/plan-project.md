---
description: Start the planning protocol defined in CLAUDE.md, beginning at Phase 1
---

Read `CLAUDE.md` in full before responding. It defines how we work; §2 defines how we plan.

We are starting the planning protocol. **Do not write code, do not create files, and do not
propose an implementation.** Start at **Phase 1 — Planning Inventory** and stop there.

Produce the inventory only: a numbered list of everything that must be decided before
implementation can begin, grouped by area. These are open questions, not answers.

For each item state:
- what has to be decided
- why it must be decided before implementation, not during it
- which requirement, deliverable, or acceptance criterion it comes from

Separately list every ambiguity or gap you found in the requirements — anything that could
reasonably be read more than one way — as its own item to be resolved and recorded as an
explicit assumption.

Do not resolve any item yet. Do not express preferences. Do not skip ahead to Phase 2.
End by asking for approval of the inventory, and flag anything you believe is missing from it.

$ARGUMENTS
