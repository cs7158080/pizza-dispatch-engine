# AI Log

A running record of where AI-generated proposals were **rejected, corrected, or accepted
without full verification** during development.

Everything an agent successfully wrote is already visible in the code and in the git
history. What disappears when a session closes is the reasoning that shaped it — what was
proposed, what was thrown away, and why. That is what this file preserves.

## When to add a row

Add a row when, and only when, one of these happens:

1. An agent proposal was **rejected** or **materially changed**.
2. Review **caught an error** in generated output.
3. Something was **accepted without full verification** — so it is known what to revisit.

## When not to add a row

Routine accepted output. "The agent wrote the schemas", "the agent helped with the
Dockerfile" — these carry no information and dilute the rows that do.

## Format

One row per event. Keep each cell to a single line.

| Date | Area | Agent proposed | Decision | Why |
|---|---|---|---|---|

`Decision` is one of: `Rejected`, `Changed`, `Accepted — unverified`.

---

## Log

| Date | Area | Agent proposed | Decision | Why |
|---|---|---|---|---|
| 2026-08-07 | plan/future-work | FW11 — a future-work entry for drivers carrying more than one order | Rejected | Item 5.8 already records the decision and everything it would reopen; a second entry duplicates it |
| 2026-08-07 | plan/scope-ceiling | Item 1.1 to auto-write a Part 5 entry whenever a candidate fails the ceiling test | Changed | Writing the entry is a file change and needs approval per §1; the rule fixes the verdict, not the authority to record it |
| 2026-08-07 | plan/structure | Splitting the planning record into three files by topic | Changed | Cross-references cluster by risk, not by topic, so a topic split would scatter them; split by role instead — questions, answers, plan |
| 2026-08-07 | plan/structure | Reconstructing the question form of the 31 items decided before the inventory was separated | Rejected | Writing a question after knowing its answer is fiction; the gap is stated in the file header instead |
| 2026-08-09 | plan/broker-contract | No retry at all on a failed publish, on the grounds that a down broker stays down longer than any request timeout | Changed | It conflated two failures; a stale long-lived connection is the common one and a single reconnect resolves it, so 7.5 retries once after reconnecting |
| 2026-08-09 | plan/cli | A single `Advance to <next>` action, with the client computing the next status from the transition sequence | Changed | The developer replaced it with a full status menu: the client must hold no part of a rule the core owns, and a reviewer must be able to trigger a `409` from the CLI itself |
| 2026-08-09 | plan/db-access | Imperative mapping costs ~80 lines more than declarative | Changed | The count was wrong by roughly six times — `map_imperatively` needs an explicit `Table`, so only the two mapper functions are saved; the developer's pushback surfaced it, and the corrected near-zero figure is what actually decided 2.5 |
| 2026-08-09 | plan/db-access | Four imprecise claims in the 2.5 draft: `session.get()` never issues a SELECT; Core yields `Row[Any]`; `Session` checks out a connection on creation; `pool_pre_ping` answers Compose startup | Changed | An external reviewer the developer consulted caught all four; each was narrowed to the condition that actually holds. A fifth suggestion — to rest the imperative-mapping rejection on framework coupling — was declined: that mapping adds no import to `domain/`, so the argument fails on inspection |
| 2026-08-09 | plan/architecture | That the event type has no home in the plan, so the publisher port would name a type defined nowhere | Changed | The developer pointed at the `outbox`: 3.5 already places an `OutboxStore` port in `application/ports.py`, so a core-side consumer of the type was already written. The finding narrowed to a placement contradiction — U6 holds "event payload type" while U5 and the U3 ports both need it — and moved from 3.4's blockers to 3.4's agenda |
| 2026-08-10 | plan/process | Closing all 109 inventory items before the first line of code, per the global Phase 3 gate in `02-decisions.md` | Changed | The gate contradicted Part 4 of the roadmap, which already gates each unit on its own topics; the global wording was the newer error and was narrowed. Planning now runs per unit — cost of change decides how early an item is settled, not the calendar. At the observed rate the remaining 54 items would have consumed the budget and left the compose environment and the test suite, the two most-graded rows, for the last hours |
| 2026-08-10 | plan/docker-images | For 3.7, a single image holding everything, resting on 1.1's ceiling test to delete a second build stage | Changed | The developer asked for alignment with what is actually done in production. 1.1's test governs shipped features, not build hygiene — applied that way it would also delete `.dockerignore` and the non-root user. The argument was withdrawn rather than defended, and 3.7 carries a runtime/test split |
| 2026-08-10 | plan/api-contract | Keeping 6.6's 50-order cap on `GET /orders` while designing 3.4's `list_recent(limit)` port method | Changed | The developer asked what happens to the 60th order. The cap had no recorded justification — 6.6 marked it "chosen, not required" — and a cap without paging silently hides the data the endpoint exists to surface. Cap removed; the port method lost its `limit` parameter with it |
| 2026-08-10 | plan/process | Rewriting the Phase 3 gate as a four-line rule inside `02-decisions.md`'s status section, on the strength of a one-word reply | Changed | The developer caught it after the commit. §1 requires explicit approval for the specific change and a one-word reply is not it — the bar is higher for a rule that governs the whole project than for a decision record. The placement was wrong too: a planning file cannot amend the working agreement, and the status section is declared the place item status is recorded. Reverted; the rule now lives in `CLAUDE.md` §2 and the status section carries a one-line pointer |
| 2026-08-10 | plan/dependencies | For 2.9, `pip-tools` as the lock generator, with `uv` rejected on local-tooling friction | Changed | The developer develops locally with `uv`. The swap costs nothing — `uv pip compile` emits a plain pip requirements file, so the image still sees only `pip` — and it removed a defect the proposal carried: both generators resolve for the host platform, so a lock built on Windows for a Linux image can carry Windows-only packages. `uv pip compile --universal` has no `pip-tools` equivalent |
| 2026-08-10 | plan/git-workflow | For 14.2, squash merges, argued from the claim that a merge node adds no information | Changed | The claim was false -- a merge node carries the unit boundary -- and the developer's pushback surfaced a contradiction under it: §2, §8 and §4 put the commit at step grain while Part 4 of the roadmap put it at unit grain, which 14.4 then settled. The record moved to merge commits and back to squash once the count was done honestly: a merge-commit main carries about eighty entries against fourteen, and GitHub's commit list is the first surface a reviewer opens. Squash stands on signal density; the original argument is withdrawn |
| 2026-08-10 | plan/git-workflow | For 14.3, a single planning commit at the head of each unit's own branch | Changed | The developer asked to plan several units in parallel across git worktrees. Under the original form a unit's Phase 2 decisions would sit on a long-lived branch and never reach main, so parallel sessions could not see each other's contracts. Phase 2 now merges promptly on its own gate branch; Phase 3 stays at the head of the unit branch. Part 4's rule 4 was narrowed with it -- it guards implementation plans from going stale, which system-level decisions cannot |
| 2026-08-11 | plan/dependencies | For 2.9, "4.7 already declined the 3.14 pull" as the whole justification for the upper version bound | Changed | 4.7 declined a third-party `uuid7()` dependency and left 3.14 explicitly open, deferring the version to 2.9 — so each record cited the other and neither decided the bound. The developer's question, why 3.14 was *not good* rather than not needed, surfaced it. 2.9 now decides it on PEP 649/749: the whole stack reads annotations at runtime, and 3.14 changed that mechanism. The earlier framing, that a declined 3.14 proved currency, is withdrawn |
| 2026-08-11 | plan/future-work | FW4 as an unbuilt `GET /orders` carrying status filters and paging | Changed | 6.6 ships the endpoint and 1.1's worked-examples table marks it *ships*, so Part 5 understated the delivered scope — in the file a reviewer reads for the scope boundary, and the one 13.4 assembles the README trade-offs from. The developer caught it while reviewing U1's plan. Narrowed to filters and paging; the rewrite erases the stale sentence, so the drift would otherwise leave no trace |
| 2026-08-11 | plan/tooling | For 2.8, ruff with `extend-select = ["I"]` and nothing else, leaving its file scope unstated | Changed | Ruff 0.16 formats `python` blocks inside Markdown, so `ruff format --check .` failed on `02-decisions.md`'s pseudo-code at step 1 — a Definition-of-Done command failing on a planning document, not on source. The plan had no ruling and step 2 forbids formatter settings, so implementation stopped rather than improvising. 2.8 now fixes the scope with `extend-exclude = ["*.md"]`; reformatting the snippets was rejected, since it edits decided records to satisfy a formatter meant for source |
| 2026-08-11 | plan/configuration | For 10.5, that the local credentials are not secrets, resting on the disposable environment alone | Changed | The developer asked what happens if named volumes are ever kept. The claim rests on two legs, not one — disposability (A19) and no published port carrying them (11.8, open) — and volumes break the first while publishing 5432 breaks the second. 10.5 now carries an explicit reopen condition naming both, pointing at FW13's second point |
| 2026-08-11 | plan/tooling | For 2.8, `extend-select = ["I"]` layered on "ruff's default — E4, E7, E9, F" | Changed | The description was false for the pinned ruff 0.16.2, whose default set is far wider and includes `UP`, so rules nobody chose were being enforced. It surfaced mid-step in U2, where `UP047` demanded PEP 695 type parameters for a generic function — firing only because 2.9 pinned `requires-python = ">=3.12"`, which ruff reads as its target version, and staying silent through U1 because U1 wrote no generic function. The set is now named explicitly with `select`, so it is what 2.8 chose in any ruff version; the accepted loss is whatever the wider default would have caught |
| 2026-08-11 | plan/git-workflow | For 14.3, merging a Phase 2 gate branch to `main` "promptly", as soon as its items close | Changed | The developer separated writing from merging: a gate is written whenever a session is free and merged when its contract is first needed. Prompt merging bought visibility between parallel sessions only, while planning runs ahead of building — so a gate could land up to five units before the unit it opens, and `main` would stop reading as "decide, then build" in a history that is itself a graded deliverable. The `Total` cell is recounted from the merged file at merge time for the same reason: every gate increments that one cell, so a count carried from a branch is stale by definition |
| 2026-08-11 | plan/configuration | For 10.3, documenting the two Compose-assembled URLs in `.env.example` as commented `# NAME=value` lines | Changed | The developer asked why they were there. In an env file that form reads as "uncomment to use", which is exactly false — Compose assembles the value, so setting it in the file has no effect. Replaced with one prose comment naming both variables, and the drift test now scans `PIZZA_*` tokens anywhere in the file instead of parsing line forms |
| 2026-08-11 | plan/broker-topology | For 7.1, `pizza.orders` for the exchange against `order.ready` for the queue, and `mandatory` rejected on the grounds that it needs a return callback | Changed | The developer asked what the dotted hierarchy in the names signified, which exposed that the two objects share no root and read as unrelated systems. The queue was renamed for its consumer under the exchange's root, following the convention that a queue names its owner and a routing key names the event. The same check felled the callback claim — it holds for pika's asynchronous adapter, not for `BlockingConnection`, where an unroutable message surfaces as `UnroutableError` — so `mandatory=True` went in |
| 2026-08-11 | plan/broker-serialization | For 7.3, an asymmetric pair — `to_json` returning `str` against `from_json` taking `bytes` | Changed | The developer pointed at the contradiction. Beyond the pair not round-tripping, the signature contradicted another claim in the same draft: decoding UTF-8 outside the function raises `UnicodeDecodeError` before the typed error is ever thrown, so 8.4's poison-message path would catch two families of error instead of one. Both directions moved to `bytes`, and the module now owns the whole conversion from untrusted input to a valid event |
| 2026-08-11 | plan/git-workflow | A deterministic three-tier branch naming scheme — `plan/u<N>-<slug>`, `<type>/u<N>-<slug>`, matching worktree directories — offered when the developer asked whether names could carry the unit's description | Rejected | She asked whether it was worth doing at all, which was the right question: 14.2 merges by squash, so a branch name appears nowhere in `main` and is visible only while the branch is open, where the unit number alone already distinguishes it. The scheme would have amended 14.3 for nothing a reviewer ever sees. Withdrawn by me; `plan/u<N>-gate` stands |
| 2026-08-11 | plan/roadmap | U3's *Decided by* column with no topic 7 item, and topic 7 filed whole under U6 | Changed | Found while opening U3: its content includes the `ORDER_READY` event type, 3.4 places `OrderReadyEvent` in `application/events.py`, 7.2 fixes its fields, and two of the five ports in that same record take one — so the gate rule would have called U3 ready to plan with an item it cannot be written without. The table assumed a topic belongs to one unit, which 7.2's own *Realised in* line already denied. The developer then asked whether U3 depending on U6 was an inverted dependency, which showed the fix was half: U6's cell claimed the same contract, and was narrowed to topic 7 minus 7.2 |
| 2026-08-11 | plan/testing | That U3 writing the unit tests its steps' DoD requires needs no change to a decided record | Changed | 5.7's *Realised in* line names U4, so U3 writing that same set amends a row rather than filling a silence — the distinction 14.7 drew when the `pytest` row moved from U4 to U2, and the one U2 §5 turned into a boundary. The substance stands: §8.2 leaves U3 no option. Recorded on the unit branch rather than a gate branch of its own, by the same ruling that placed U2's commit A |
| 2026-08-11 | plan/architecture | For U3's step 5, catching a failed outbox mark as `except Exception`, argued as not the swallowing §6 rejects because it logs at ERROR and leaves the unpublished row behind | Changed | The log and the row hold; the consequence does not. Our own bug lands in the same net and leaves a row asserting the event was lost when it was published, so 7.5's record lies exactly where it is meant to be trusted. 3.4 already carried the pattern and had not applied it — `PublishFailed` sits beside `EventPublisher` because an unreachable broker is not a violated rule, and a failed write is not one either. The developer's two questions, why not `domain/errors.py` and why `application/` has no errors module, turned it into a stated placement rule with a trigger condition |
| 2026-08-12 | plan/data-model | Three names for one value across the chain — `to` on the entity method per 4.9, `requested_status` on the use case, `requested` on the error — defended as different vantage points | Changed | The developer rejected the defence and she was right: one concept, one name. `to` is clear only beside `advance_to` and vague at every other site, and `next_status` would be a lie, since asking for a non-adjacent status is a supported path that 5.2 exists for and 9.2 makes ordinary at the CLI. 4.9's sample is amended for the name alone; the error's attributes needed no amendment, because 5.2 names its contents and not their spelling |
| 2026-08-12 | style/comments | Source comments carrying rejected alternatives, reopen conditions, cost analysis and planning item numbers, through all thirteen modules of U3 | Changed | The developer asked for comments that state purpose and logic briefly, and the objection holds: rationale has a home in `.claude/plans`, and the copy in the source is the one that goes stale — the failure the decisions file already names about a fact written twice. What stays is what a caller must know to use the thing correctly, including the two docstrings 3.2 requires. U2's three files were brought to the same standard in the same pass, since one exception becomes the model the next session imitates |
| 2026-08-12 | style/comments | The cleanup pass itself: it stripped the item numbers from `DispatchOutcome` and `AssignmentState` and took their meaning with them, and left twenty docstring lines above the 88 columns the formatter does not reflow | Changed | A parallel session reviewed the result and all ten of its findings held on inspection. The substantive one is that the previous commit cut past its own rule — a value the worker maps to an acknowledgement policy is what a caller must know, not rationale — so the enums are documented again without citations. The width drifted invisibly because `ruff format` reflows code and not prose, and `E501` sits in a pycodestyle family 2.8 deliberately did not select |
| 2026-08-12 | plan/schema-creation | For 4.6, declining Alembic with a `revisit if` line inside the decision record and no Part 5 entry | Changed | The developer asked for it as future work conditioned on named volumes, which are themselves conditioned on FW13 — and that chain is the entry's content: a migration tool needs data that survives a launch, and 11.7's disposability reopens only through an isolated test environment. A reopen condition buried in `02-decisions.md` also answers the likeliest interview question about persistence in the file a reviewer reads last, where Part 5 is the one written for it. Added as FW16 |
| 2026-08-12 | plan/roadmap | 7.3's serialization module filed under U6 while U5 needs it to write the outbox `payload`, with U5 depending on U2 and U3 only | Changed | Found while opening U5, and the same shape as the 7.2 finding at U3's gate: three records give the module to U6 — 7.3's *Realised in*, 3.4's closing sentence and the U6 content cell — while two others have U5 calling it, and 7.3's stdlib-only rule exists precisely so the database adapter can import it. Making U5 depend on U6 was rejected: it reverses the spine for one file. U5 writes the module, U6 and U8 import it, and it stays under `broker/` because the format is the wire contract — the outbox stores a copy of the message. The developer asked what would reverse that, so 7.3 gains a `Revisit if` naming a non-wire reader, which also reopens its no-version decision once rows outlive the code |
| 2026-08-12 | plan/broker-serialization | For 7.3, the database adapter writing `serialize(event).decode("utf-8")` into 4.5's `jsonb` column | Changed | Caught while writing U5's models, by checking the bind processor rather than trusting the record: SQLAlchemy's `JSONB` runs `json.dumps` over whatever it is given, so a string already holding JSON is encoded twice and the row keeps a JSON string instead of an object. The `INSERT` succeeds either way and `payload ->> 'order_id'` — the single query 4.5 keeps the column for — returns null, so the fault would have surfaced only when someone came to read the evidence of a lost dispatch. The expression is now `json.loads(serialize(event).decode("utf-8"))`: one producer still makes both copies, which is all 7.3 decided, and 4.5 is untouched. A `text` column and a custom SQLAlchemy type were both rejected — the first loses the query, the second is machinery for the same result |
| 2026-08-12 | infrastructure/outbox | An explicit `session.flush()` inside `mark_published`, so that a failed write is raised by the method the caller wrapped rather than by the later `commit()` | Rejected | The developer judged it more machinery than the case is worth, and the reasoning holds at the size the failure actually has: it needs a database fault between two adjacent lines, and its cost is a `500` on a request that succeeded — the row is unmarked either way, which 7.5 already accepts. The counter-argument, that the port declares `OutboxWriteFailed` and lazy writes leave that declaration empty, is the reason the window is written into U5's plan and handed to U7's gate instead: what `UnitOfWork.commit()` may raise is a contract 3.4 and 3.5 left silent, and patching one call site would have settled it by accident |
| 2026-08-12 | plan/data-model | Keeping 4.5's `jsonb` for `orders.items`, on the record's own tie-breaker — one JSON type in the schema instead of two | Changed | The developer asked why the column is not `text[]`, and the tie-breaker does not survive the question: it counted type names rather than concepts, since `payload` is a document and `items` is a list of strings under either spelling. What it was trading away is that the repository already declares `Mapped[list[str]]` while `jsonb` would accept an object, a number or a scalar in that column with nothing to notice. Changed to `text[]`, which makes the annotation a statement the database enforces. Her same instinct applied to `VARCHAR(n)` on the text columns was declined and the reason is the distinction the exchange produced: a type does not drift, a bound written twice does — 4.2's 100 and 200 would then live in two places, and 12.3 leaves no test that could catch them diverging |
| 2026-08-12 | plan/broker-lifecycle | For 7.7, `socket_timeout` and `blocked_connection_timeout` as the two parameters that bound a publish in time, with `socket_timeout` covering "connection establishment and socket operations" | Changed | Caught while opening U6, by reading pika 1.4.4 rather than trusting the record: `socket_timeout` is documented as the `socket.connect()` timeout alone, and the full TCP/handshake/channel bring-up is bounded by a third parameter, `stack_timeout`, whose default is 15 s. A broker that answers the TCP connect and then stalls the handshake would have held a `PATCH` for 30 s against the 10 s 7.5 promises — the bound named in the record and the bound in force differing by three times, in the one item written to fix exactly that. All three are now fed from 10.4's single timeout; no new variable, and 10.4 is untouched |
| 2026-08-12 | plan/broker-lifecycle | Recording the unbounded wait for a publisher confirm as an accepted cost inside 7.7 | Changed | The developer asked for it as future work instead, and that is the better home: Part 5 is the register §7 requires for deliberate exclusions, and it is the file a reviewer opens to ask what comes next — where "what happens if the broker accepts your connection and then goes silent" is a likely question. Written as FW17, which also records what the prose would have missed: there are two routes of different sizes, and FW12's async runtime closes it completely while the dedicated publisher thread frees the caller and leaves the thread stuck |
| 2026-08-12 | plan/api-contract | For 6.1, registering its new assumption as A25 | Changed | Caught while rebasing the gate onto main: topic 10 closed in PR #6 and took A25 and A26, so the number was free only at the branch's base. The briefing's merge rule — resolve outside topics 6 and 8 in favour of main — would have dropped the assumption rather than renumbered it. Now A27. The same collision recurs on `plan/u8-gate`, whose 8.4 also picked A27 and becomes A28 at its own merge |
