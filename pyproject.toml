[project]
name = "pizza"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "pydantic>=2",
    "uvicorn",
    "sqlalchemy>=2.0",
    "psycopg[binary]",
    "pika",
    "httpx",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "ruff",
    "mypy",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.ruff]
extend-exclude = ["*.md"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "E501", "F", "I", "TID251"]

# 3.1's dependency rule: the core names no third-party library.
[tool.ruff.lint.flake8-tidy-imports.banned-api]
"sqlalchemy".msg = "infrastructure only — 3.1"
"pika".msg = "infrastructure only — 3.1"
"fastapi".msg = "infrastructure only — 3.1"
"pydantic".msg = "config.py and entrypoints only — 3.1"

[tool.ruff.lint.per-file-ignores]
"src/pizza/infrastructure/*" = ["TID251"]
"src/pizza/entrypoints/*" = ["TID251"]
"src/pizza/config.py" = ["TID251"]
"tests/*" = ["TID251"]

[tool.mypy]
strict = true
